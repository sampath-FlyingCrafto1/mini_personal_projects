This Python script generates ASCII art banners from user-provided text.

**Features:**

- Uses the `pyfiglet` library to create visually appealing ASCII art.
- Allows users to input text to be displayed as an ASCII banner.
- Provides a user-friendly interface with a simple banner and an option to run the program again.

**Requirements:**

- `pyfiglet` library: `pip install pyfiglet`